#include<stdio.h>

int main ()
{
    int num,fact;
    printf("ENTER A NUMBER:");
    scanf("%d",&num);

    fact= factorial(num);
    printf("THIS FACTORIAL OF%d IS %d:",num,fact);
    return 0;
}

    int factorial(int x)
{
    if(x==1)
        return 1;
   return x*factorial(x-1);

}


